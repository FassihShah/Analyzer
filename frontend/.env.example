# Portfolio mode: synthetic data stored only in each visitor's browser.
NEXT_PUBLIC_DEMO_MODE=true

# For the real backend instead, set NEXT_PUBLIC_DEMO_MODE=false and supply
# a public HTTPS API endpoint. Never put secrets in NEXT_PUBLIC_ variables.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api
